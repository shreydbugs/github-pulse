# GitHub Token (Optional but highly recommended to avoid strict rate limits)
# Create a fine-grained token with "Public Data" access only at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Backend Port (Default: 3001)
PORT=3001

# CORS configuration
CORS_ORIGIN=*
